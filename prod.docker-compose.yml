version: "3"
services:
  app:
    image: skyride/reve-flairs:1.1.4
    restart: always
    env_file:
      - .env
    volumes:
      - media:/media
    ports:
      - ${PORT}:${PORT}

volumes:
  media:
